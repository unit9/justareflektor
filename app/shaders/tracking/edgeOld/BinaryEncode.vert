varying mediump vec2 redUV;
varying mediump vec2 greenUV;
varying mediump vec2 blueUV;
varying mediump vec2 alphaUV;

void main() {

	highp vec2 vUv;

	vUv.y = uv.x - 0.5

	gl_Position = projectionMatrix *  modelViewMatrix * vec4( position, 1.0 );
}	